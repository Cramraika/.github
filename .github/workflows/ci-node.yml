# Reusable Node CI — call from a thin per-repo caller:
#
#   jobs:
#     ci:
#       uses: Cramraika/.github/.github/workflows/ci-node.yml@main
#       with:
#         runner: ${{ vars.RUNNER_TYPE || 'ubuntu-latest' }}
#         node-version: "20"
#         run-tests: true
#         docker-build: false
#       secrets: inherit
#
# Preserves the project-template ci-node behavior (tolerant install, build --if-present,
# CI: false so CRA-style warnings don't fail the build).
name: Reusable Node CI

on:
  workflow_call:
    inputs:
      runner:
        description: "Runner label (caller resolves vars.RUNNER_TYPE || 'ubuntu-latest')."
        required: false
        type: string
        default: ubuntu-latest
      node-version:
        description: "Node version."
        required: false
        type: string
        default: "20"
      run-tests:
        description: "Run `npm test` (skips gracefully if no test script)."
        required: false
        type: boolean
        default: true
      docker-build:
        description: "Run a no-push docker build smoke if a Dockerfile exists."
        required: false
        type: boolean
        default: false

jobs:
  ci:
    name: Node CI
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # setup-node's runtime download is unreliable on self-hosted Mac runners.
      # Tolerate its failure and fall back to the runner's system node.
      - name: Set up Node
        id: setup-node
        uses: actions/setup-node@v4
        continue-on-error: true
        with:
          node-version: ${{ inputs.node-version }}

      - name: Ensure a usable Node (self-hosted fallback)
        run: |
          if ! command -v node >/dev/null 2>&1; then
            echo "::error::No node on PATH and setup-node failed."
            exit 1
          fi
          node --version && npm --version

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit --prefer-offline 2>/dev/null || npm install --no-audit --prefer-offline 2>/dev/null || npm install --legacy-peer-deps --no-audit
          else
            npm install --no-audit --prefer-offline 2>/dev/null || npm install --legacy-peer-deps --no-audit
          fi

      - name: Lint (if script present)
        run: |
          if node -e "process.exit(require('./package.json').scripts?.lint ? 0 : 1)" 2>/dev/null; then
            npm run lint
          else
            echo "No lint script — skipping."
          fi

      - name: Typecheck (if script present)
        run: |
          if node -e "process.exit(require('./package.json').scripts?.typecheck ? 0 : 1)" 2>/dev/null; then
            npm run typecheck
          else
            echo "No typecheck script — skipping."
          fi

      - name: Build
        run: npm run build --if-present
        env:
          CI: false

      - name: Test
        if: ${{ inputs.run-tests }}
        run: |
          if node -e "process.exit(require('./package.json').scripts?.test ? 0 : 1)" 2>/dev/null; then
            npm test
          else
            echo "No test script — skipping."
          fi

      - name: npm audit (high+, advisory)
        continue-on-error: true
        run: npm audit --audit-level=high 2>&1 | tail -30 || true

      - name: Docker build smoke (no push)
        if: ${{ inputs.docker-build }}
        run: |
          if [ -f Dockerfile ]; then
            docker build -t ci-smoke:${{ github.sha }} .
          else
            echo "No Dockerfile — skipping docker build smoke."
          fi
